// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles
parcelRequire = (function (modules, cache, entry, globalName) {
  // Save the require from previous bundle to this closure if any
  var previousRequire = typeof parcelRequire === 'function' && parcelRequire;
  var nodeRequire = typeof require === 'function' && require;

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire = typeof parcelRequire === 'function' && parcelRequire;
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error('Cannot find module \'' + name + '\'');
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = cache[name] = new newRequire.Module(name);

      modules[name][0].call(module.exports, localRequire, module, module.exports, this);
    }

    return cache[name].exports;

    function localRequire(x){
      return newRequire(localRequire.resolve(x));
    }

    function resolve(x){
      return modules[name][1][x] || x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [function (require, module) {
      module.exports = exports;
    }, {}];
  };

  var error;
  for (var i = 0; i < entry.length; i++) {
    try {
      newRequire(entry[i]);
    } catch (e) {
      // Save first error but execute all entries
      if (!error) {
        error = e;
      }
    }
  }

  if (entry.length) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(entry[entry.length - 1]);

    // CommonJS
    if (typeof exports === "object" && typeof module !== "undefined") {
      module.exports = mainExports;

    // RequireJS
    } else if (typeof define === "function" && define.amd) {
     define(function () {
       return mainExports;
     });

    // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }

  // Override the current require with this new one
  parcelRequire = newRequire;

  if (error) {
    // throw error from earlier, _after updating parcelRequire_
    throw error;
  }

  return newRequire;
})({"components/Menu.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function hasSubMenu($) {
  var arrow = '<i class="fas fa-chevron-down"></i>';
  var item = $(".nav li.menu-item-has-children > a");
  item.append(arrow);
}

var _default = hasSubMenu;
exports.default = _default;
},{}],"components/SmoothScroll.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function smoothScroll($) {
  $('a[href*="#"]').on("click", function (e) {
    if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
      e.preventDefault();
      var target = $(this.hash);
      var nav = $(".shiftnav-shiftnav-main");

      if (target.selector.includes("newsletter")) {
        location.href = "https://ogorek.com/contact/";
      }

      if (nav) {
        nav.removeClass("shiftnav-open-target");
        $("body").removeClass("shiftnav-open-left");
        $("body").removeClass("shiftnav-open");
        $("body").addClass("shiftnav-transitioning");
      }

      if (target.offset()) {
        $("html, body").animate({
          scrollTop: $("body").hasClass("page-id-564") ? target.offset().top - 210 : target.offset().top - 170
        }, 500, "linear");
      }
    }
  });
}

var _default = smoothScroll;
exports.default = _default;
},{}],"components/Videos.js":[function(require,module,exports) {
// Add placeholder to videos
function addPlaceholder($) {
  var video = $("video") || $("iframe");
  var defaultPlaceholderImg = "http://ogorek.com/wp-content/uploads/2018/11/7d1119db3034129b5863b4238d3cca58ea9e35d4-min.jpg";
  var placeholderSet = $("#span-placeholder").attr("data-src");
  var placeholderImg = placeholderSet ? placeholderSet : defaultPlaceholderImg;
  var placeholder = $('<div style="background-image:url(' + placeholderImg + ')" class="placeholder"></div>');

  if (video.length > 1) {
    $.each(video, function (index, el) {
      if ($(el).attr("name") && $(el).attr("name").match("gform")) return;
      $(el).addClass("yt-video");
      $(el).wrap('<div class="video"></div>');
      $(el).after(placeholder);
    });
  } else {
    if (video.attr("name") && video.attr("name").match("gform")) return;
    video.addClass("yt-video");
    video.wrap('<div class="video"></div>');
    video.after(placeholder);
  }
} // Remove placeholder


function showVideo($) {
  var placeholder = $(".placeholder");
  placeholder.on("click", function (e) {
    placeholder.addClass("fadeOut");
    setTimeout(function () {
      placeholder.remove();
    }, 700);
  });
} // Add preload to videos embedded in content


function addPlaceholderInternalPages($) {
  var videos = $(".entry-content .wp-video-shortcode source");

  if (videos.length > 0) {
    $.each(videos, function (index, video) {
      var src = $(video).attr("src");
      var removeQueryString = src.split("?")[0];
      var formattedSrc = removeQueryString + "#t=0.1";
      $(video).attr("src", formattedSrc);
    });
  }
} // Defer loading of videos


function loadIframe() {
  if (!jQuery("body").hasClass("home")) return;
  var vidDefer = document.getElementsByTagName("iframe");

  for (var i = 0; i < vidDefer.length; i++) {
    if (vidDefer[i].getAttribute("data-src")) {
      vidDefer[i].setAttribute("src", vidDefer[i].getAttribute("data-src"));
    }
  }
}

module.exports = {
  loadIframe: loadIframe,
  addPlaceholder: addPlaceholder,
  addPlaceholderInternalPages: addPlaceholderInternalPages,
  showVideo: showVideo
};
},{}],"../../node_modules/core-js/library/modules/_global.js":[function(require,module,exports) {

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
var global = module.exports = typeof window != 'undefined' && window.Math == Math
  ? window : typeof self != 'undefined' && self.Math == Math ? self
  // eslint-disable-next-line no-new-func
  : Function('return this')();
if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef

},{}],"../../node_modules/core-js/library/modules/_core.js":[function(require,module,exports) {
var core = module.exports = { version: '2.6.11' };
if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef

},{}],"../../node_modules/core-js/library/modules/_a-function.js":[function(require,module,exports) {
module.exports = function (it) {
  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
  return it;
};

},{}],"../../node_modules/core-js/library/modules/_ctx.js":[function(require,module,exports) {
// optional / simple context binding
var aFunction = require('./_a-function');
module.exports = function (fn, that, length) {
  aFunction(fn);
  if (that === undefined) return fn;
  switch (length) {
    case 1: return function (a) {
      return fn.call(that, a);
    };
    case 2: return function (a, b) {
      return fn.call(that, a, b);
    };
    case 3: return function (a, b, c) {
      return fn.call(that, a, b, c);
    };
  }
  return function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};

},{"./_a-function":"../../node_modules/core-js/library/modules/_a-function.js"}],"../../node_modules/core-js/library/modules/_is-object.js":[function(require,module,exports) {
module.exports = function (it) {
  return typeof it === 'object' ? it !== null : typeof it === 'function';
};

},{}],"../../node_modules/core-js/library/modules/_an-object.js":[function(require,module,exports) {
var isObject = require('./_is-object');
module.exports = function (it) {
  if (!isObject(it)) throw TypeError(it + ' is not an object!');
  return it;
};

},{"./_is-object":"../../node_modules/core-js/library/modules/_is-object.js"}],"../../node_modules/core-js/library/modules/_fails.js":[function(require,module,exports) {
module.exports = function (exec) {
  try {
    return !!exec();
  } catch (e) {
    return true;
  }
};

},{}],"../../node_modules/core-js/library/modules/_descriptors.js":[function(require,module,exports) {
// Thank's IE8 for his funny defineProperty
module.exports = !require('./_fails')(function () {
  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
});

},{"./_fails":"../../node_modules/core-js/library/modules/_fails.js"}],"../../node_modules/core-js/library/modules/_dom-create.js":[function(require,module,exports) {
var isObject = require('./_is-object');
var document = require('./_global').document;
// typeof document.createElement is 'object' in old IE
var is = isObject(document) && isObject(document.createElement);
module.exports = function (it) {
  return is ? document.createElement(it) : {};
};

},{"./_is-object":"../../node_modules/core-js/library/modules/_is-object.js","./_global":"../../node_modules/core-js/library/modules/_global.js"}],"../../node_modules/core-js/library/modules/_ie8-dom-define.js":[function(require,module,exports) {
module.exports = !require('./_descriptors') && !require('./_fails')(function () {
  return Object.defineProperty(require('./_dom-create')('div'), 'a', { get: function () { return 7; } }).a != 7;
});

},{"./_descriptors":"../../node_modules/core-js/library/modules/_descriptors.js","./_fails":"../../node_modules/core-js/library/modules/_fails.js","./_dom-create":"../../node_modules/core-js/library/modules/_dom-create.js"}],"../../node_modules/core-js/library/modules/_to-primitive.js":[function(require,module,exports) {
// 7.1.1 ToPrimitive(input [, PreferredType])
var isObject = require('./_is-object');
// instead of the ES6 spec version, we didn't implement @@toPrimitive case
// and the second argument - flag - preferred type is a string
module.exports = function (it, S) {
  if (!isObject(it)) return it;
  var fn, val;
  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
  throw TypeError("Can't convert object to primitive value");
};

},{"./_is-object":"../../node_modules/core-js/library/modules/_is-object.js"}],"../../node_modules/core-js/library/modules/_object-dp.js":[function(require,module,exports) {
var anObject = require('./_an-object');
var IE8_DOM_DEFINE = require('./_ie8-dom-define');
var toPrimitive = require('./_to-primitive');
var dP = Object.defineProperty;

exports.f = require('./_descriptors') ? Object.defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPrimitive(P, true);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return dP(O, P, Attributes);
  } catch (e) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};

},{"./_an-object":"../../node_modules/core-js/library/modules/_an-object.js","./_ie8-dom-define":"../../node_modules/core-js/library/modules/_ie8-dom-define.js","./_to-primitive":"../../node_modules/core-js/library/modules/_to-primitive.js","./_descriptors":"../../node_modules/core-js/library/modules/_descriptors.js"}],"../../node_modules/core-js/library/modules/_property-desc.js":[function(require,module,exports) {
module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};

},{}],"../../node_modules/core-js/library/modules/_hide.js":[function(require,module,exports) {
var dP = require('./_object-dp');
var createDesc = require('./_property-desc');
module.exports = require('./_descriptors') ? function (object, key, value) {
  return dP.f(object, key, createDesc(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};

},{"./_object-dp":"../../node_modules/core-js/library/modules/_object-dp.js","./_property-desc":"../../node_modules/core-js/library/modules/_property-desc.js","./_descriptors":"../../node_modules/core-js/library/modules/_descriptors.js"}],"../../node_modules/core-js/library/modules/_has.js":[function(require,module,exports) {
var hasOwnProperty = {}.hasOwnProperty;
module.exports = function (it, key) {
  return hasOwnProperty.call(it, key);
};

},{}],"../../node_modules/core-js/library/modules/_export.js":[function(require,module,exports) {

var global = require('./_global');
var core = require('./_core');
var ctx = require('./_ctx');
var hide = require('./_hide');
var has = require('./_has');
var PROTOTYPE = 'prototype';

var $export = function (type, name, source) {
  var IS_FORCED = type & $export.F;
  var IS_GLOBAL = type & $export.G;
  var IS_STATIC = type & $export.S;
  var IS_PROTO = type & $export.P;
  var IS_BIND = type & $export.B;
  var IS_WRAP = type & $export.W;
  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
  var expProto = exports[PROTOTYPE];
  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] : (global[name] || {})[PROTOTYPE];
  var key, own, out;
  if (IS_GLOBAL) source = name;
  for (key in source) {
    // contains in native
    own = !IS_FORCED && target && target[key] !== undefined;
    if (own && has(exports, key)) continue;
    // export native or passed
    out = own ? target[key] : source[key];
    // prevent global pollution for namespaces
    exports[key] = IS_GLOBAL && typeof target[key] != 'function' ? source[key]
    // bind timers to global for call from export context
    : IS_BIND && own ? ctx(out, global)
    // wrap global constructors for prevent change them in library
    : IS_WRAP && target[key] == out ? (function (C) {
      var F = function (a, b, c) {
        if (this instanceof C) {
          switch (arguments.length) {
            case 0: return new C();
            case 1: return new C(a);
            case 2: return new C(a, b);
          } return new C(a, b, c);
        } return C.apply(this, arguments);
      };
      F[PROTOTYPE] = C[PROTOTYPE];
      return F;
    // make static versions for prototype methods
    })(out) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
    // export proto methods to core.%CONSTRUCTOR%.methods.%NAME%
    if (IS_PROTO) {
      (exports.virtual || (exports.virtual = {}))[key] = out;
      // export proto methods to core.%CONSTRUCTOR%.prototype.%NAME%
      if (type & $export.R && expProto && !expProto[key]) hide(expProto, key, out);
    }
  }
};
// type bitmap
$export.F = 1;   // forced
$export.G = 2;   // global
$export.S = 4;   // static
$export.P = 8;   // proto
$export.B = 16;  // bind
$export.W = 32;  // wrap
$export.U = 64;  // safe
$export.R = 128; // real proto method for `library`
module.exports = $export;

},{"./_global":"../../node_modules/core-js/library/modules/_global.js","./_core":"../../node_modules/core-js/library/modules/_core.js","./_ctx":"../../node_modules/core-js/library/modules/_ctx.js","./_hide":"../../node_modules/core-js/library/modules/_hide.js","./_has":"../../node_modules/core-js/library/modules/_has.js"}],"../../node_modules/core-js/library/modules/es6.date.now.js":[function(require,module,exports) {
// 20.3.3.1 / 15.9.4.4 Date.now()
var $export = require('./_export');

$export($export.S, 'Date', { now: function () { return new Date().getTime(); } });

},{"./_export":"../../node_modules/core-js/library/modules/_export.js"}],"../../node_modules/core-js/library/fn/date/now.js":[function(require,module,exports) {
require('../../modules/es6.date.now');
module.exports = require('../../modules/_core').Date.now;

},{"../../modules/es6.date.now":"../../node_modules/core-js/library/modules/es6.date.now.js","../../modules/_core":"../../node_modules/core-js/library/modules/_core.js"}],"../../node_modules/@babel/runtime-corejs2/core-js/date/now.js":[function(require,module,exports) {
module.exports = require("core-js/library/fn/date/now");
},{"core-js/library/fn/date/now":"../../node_modules/core-js/library/fn/date/now.js"}],"components/Popup.js":[function(require,module,exports) {
"use strict";

var _now = _interopRequireDefault(require("@babel/runtime-corejs2/core-js/date/now"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Popup functionality
function popup($) {
  var body = $("body");
  var modal = $(".popup");
  var close = $(".popup .close");
  var date = new Date((0, _now.default)() + 86400e3).toUTCString();
  var cookieString = "popShown=true; expires=" + date; // if popup was closed this session, don't show it again

  if (document.cookie.match("popShown")) {
    modal.remove();
    return;
  } // show popup


  if (modal.length > 0) {
    setTimeout(function () {
      modal.addClass("active");
      body.addClass("popup-active");
    }, 5000);
  }

  close.on("click", function (e) {
    e.preventDefault();
    modal.removeClass("active");
    body.removeClass("popup-active");
    document.cookie = cookieString;
    document.cookie = cookieString;
    setTimeout(function () {
      modal.remove();
    }, 1200);
  });
  body.on("click", function (e) {
    if (!$(e.target).closest(".inner-popup").length) {
      modal.removeClass("active");
      body.removeClass("popup-active");
      document.cookie = cookieString;
      document.cookie = cookieString;
      setTimeout(function () {
        modal.remove();
      }, 1200);
    }
  });
} // Successful newsletter signup


function newsletterSignup($) {
  jQuery(document).on("gform_confirmation_loaded", function (event, formId) {
    // code to be trigger when confirmation page is loaded
    setTimeout(function () {
      var body = $("body");
      var modal = $(".popup");
      modal.removeClass("active");
      body.removeClass("popup-active");
      localStorage.popShown = true;
      setTimeout(function () {
        modal.remove();
      }, 1200);
    }, 3500);
  });
}

module.exports = {
  newsletterSignup: newsletterSignup,
  popup: popup
};
},{"@babel/runtime-corejs2/core-js/date/now":"../../node_modules/@babel/runtime-corejs2/core-js/date/now.js"}],"components/MobileMenu.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function mobileMenuSubMenu($) {
  var navUl = $(".shiftnav-nav .shiftnav-menu");
  var parent = navUl.find(".menu-item-has-children");
  var btn = parent.children(".shiftnav-target");
  $.each(btn, function (idx, el) {
    !$(el).parent().hasClass("menu-item-has-children") ? null : $(el).html($(el).html() + " <i class='fas fa-plus'></i>");
  });
  btn.on("click", function (e) {
    e.preventDefault();
    $(e.target).parent().children(".sub-menu").slideDown(400);

    if ($(e.target).hasClass("fas")) {
      return;
    } else if (!$(e.target).hasClass("fas") && $(e.target).parent().children(".sub-menu").css("display") == "block") {
      $(e.target).unbind(e);
    }
  });
  $(btn).find(".fas").on("click", function (e) {
    $(e.target).closest(".menu-item-has-children").children(".sub-menu").slideToggle(400);
  });
}

var _default = mobileMenuSubMenu;
exports.default = _default;
},{}],"components/ScrollTop.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function scrollTop($) {
  var btn = $("#scrollTop");
  var bod = $("html, body");
  btn.on("click", function () {
    bod.animate({
      scrollTop: "0"
    });
  });
  $(window).on("scroll", function () {
    if (bod.scrollTop() >= 500) {
      btn.fadeIn(300);
    } else {
      btn.fadeOut(300);
    }
  });
}

var _default = scrollTop;
exports.default = _default;
},{}],"components/Redirects.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

// Redirect from old #newsletter to contact page since its all in one
function newsletterRedirect() {
  var urlSplit = document.URL.split("#");

  if (urlSplit[1] && urlSplit.includes("newsletter")) {
    location.href = "https://ogorek.com/contact/";
  }
}

var _default = newsletterRedirect;
exports.default = _default;
},{}],"components/ContactPage.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

// Reveal the newsletter signup form on contact page
function contactNewsletter($) {
  var btn = $("a[href*=letter]");
  var form = $("#gform_wrapper_4");
  btn.on("click", function (e) {
    e.preventDefault();
    form.show(500);
  });
}

var _default = contactNewsletter;
exports.default = _default;
},{}],"components/Blog.js":[function(require,module,exports) {
// Add zoom functionality
function addImageZoom($) {
  if ($(window).width() <= 1024) return; // For the inner post imgs

  var postImgs = $(".imgzoom").parent(".post-image-wrapper");
  $.each(postImgs, function (idx, el) {
    var postImgOptions = {
      width: $(el).width(),
      zoomPosition: "original"
    };
    new ImageZoom(el, postImgOptions);
  });
} // Add lightbox


function addLightbox($) {
  var imgs = $(".entry-content img").not(".featured-image");
  $.each(imgs, function (index, img) {
    var src = $(img).attr("src");
    $(img).wrap("<a href='" + src + "' class='foobox post-image-wrapper'></a>");
  });
}

module.exports = {
  addLightbox: addLightbox,
  addImageZoom: addImageZoom
};
},{}],"components/Slider.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function initSlider($) {
  $(".owl-carousel").owlCarousel({
    items: 1,
    nav: true,
    dots: false,
    rewind: true,
    autoplay: true,
    autoplayTimeout: 7500,
    autoplayHoverPause: true,
    lazyload: true,
    navText: ['<i class="fas fa-chevron-left slick-prev"></i>', '<i class="fas fa-chevron-right slick-next"></i>']
  });
}

var _default = initSlider;
exports.default = _default;
},{}],"components/Trust.js":[function(require,module,exports) {
// Law firm scripts
function trustChosen($) {
  var trustForm = $("#trusts");
  trustForm.change(function (e) {
    var val = $(this).val();
    var url = document.location;

    if (url.href.match("trust")) {
      var _url = document.location.href.split("&trust")[0];
      document.location = _url + "&trust=" + val;
    } else {
      document.location = url + "&trust=" + val;
    }
  }); // Update trust title

  $("#updateField").on("click", function (e) {
    var newTitle = $("#trustTitle").val();
    var url = document.location;

    if (url.href.match("title")) {
      var _url2 = document.location.href.split("&title")[0];
      document.location = _url2 + "&title=" + newTitle;
    } else {
      document.location = url + "&title=" + newTitle;
    }
  });
} // Create a new trust


function createTrust($) {
  $("#addTrust").on("click", function (e) {
    var url = document.location;
    var newTrustTitle = $("#trustTitle").val();
    var curTitle = document.querySelector("#trusts").options.namedItem("trust-" + $("#trusts").val()).text;

    if (newTrustTitle == curTitle) {
      $(".alert-error").fadeIn();
      return;
    }

    if (url.href.match("create")) {
      var _url3 = document.location.href.split("&create")[0];
      document.location = _url3 + "&create=" + newTrustTitle;
      $(".alert-error").fadeOut();
    } else {
      document.location = url + "&create=" + newTrustTitle;
      $(".alert-error").fadeOut();
    }
  });
} // Remove the create query string and redirect to self


function removeCreate($) {
  var url = document.location.href;
  setTimeout(function () {
    if (url.match("create") && !url.match("created")) {
      var urlArr = url.split("&create")[0];
      var created = url.split("&create")[1];
      window.history.pushState({
        page: 1
      }, document.title, urlArr + "&created" + created);
      window.location.reload();
    }
  }, 500);
} // Remove title query string from url and redirect back to self


function removeTitleQs($) {
  var url = document.location.href;
  var urlArr = url.split("&");
  var toRemove = "title";

  if (url.match(toRemove) !== null) {
    urlArr.forEach(function (element, index) {
      if (element.match(toRemove) !== null) {
        urlArr.splice(index, index);
        document.location = urlArr.join("&");
      }
    });
  }
} // Add check to completed form options


function addCheck($) {
  var btn = $(".trusts .form .btn.completed");
  btn.html("Form Completed" + '<i class="fas fa-check"></i>');
}

module.exports = {
  addCheck: addCheck,
  removeTitleQs: removeTitleQs,
  removeCreate: removeCreate,
  createTrust: createTrust,
  trustChosen: trustChosen
};
},{}],"components/Scheduler.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function prePopulateScheduler($) {
  var urlParams = new URLSearchParams(window.location.search);
  var firstName = urlParams.get("fname");
  var lastName = urlParams.get("lname");
  var email = urlParams.get("email");
  var phone = urlParams.get("phone");
  var scheduler = $("iframe[src*=acuity]");
  var formattedURL = "".concat(scheduler.attr("src"), "&firstName=").concat(firstName, "&lastName=").concat(lastName, "&phone=").concat(phone, "&email=").concat(email);
  scheduler.attr("src", formattedURL);
}

var _default = prePopulateScheduler;
exports.default = _default;
},{}],"components/LandingPage.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

// Landing page form toggle
function showForm($) {
  var cta = $(".get-started");
  var form = $(".form");
  var container = $(".popup-container");
  var body = $("body");
  var close = $(".form .close"); // Show form

  cta.on("click", function (e) {
    container.addClass("active");
    form.addClass("active");
    body.addClass("popup-active");
  }); // Hide form

  close.on("click", function (e) {
    e.preventDefault();
    form.removeClass("active");
    body.removeClass("popup-active");
    container.removeClass("active");
  });
}

var _default = showForm;
exports.default = _default;
},{}],"components/FAQ.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

// FAQs page
function toggleFAQ($) {
  var questions = $(".faqs__question");
  questions.on("click", function (e) {
    var self = $(e.target); // Traverses DOM until it gets the right level

    var question = self.is("span") || self.is("i") ? self.parentsUntil(".text") : self; // Setting active class to question toggled to enable animation

    question.toggleClass("active"); // Selects the correct answer to toggle the visibility of

    var answerToToggle = question.next();
    answerToToggle.slideToggle(300, function () {
      answerToToggle.toggleClass("active");
    });
  });
}

var _default = toggleFAQ;
exports.default = _default;
},{}],"scripts.js":[function(require,module,exports) {
"use strict";

var _Menu = _interopRequireDefault(require("./components/Menu"));

var _SmoothScroll = _interopRequireDefault(require("./components/SmoothScroll"));

var _Videos = require("./components/Videos");

var _Popup = require("./components/Popup");

var _MobileMenu = _interopRequireDefault(require("./components/MobileMenu"));

var _ScrollTop = _interopRequireDefault(require("./components/ScrollTop"));

var _Redirects = _interopRequireDefault(require("./components/Redirects"));

var _ContactPage = _interopRequireDefault(require("./components/ContactPage"));

var _Blog = require("./components/Blog");

var _Slider = _interopRequireDefault(require("./components/Slider"));

var _Trust = require("./components/Trust");

var _Scheduler = _interopRequireDefault(require("./components/Scheduler"));

var _LandingPage = _interopRequireDefault(require("./components/LandingPage"));

var _FAQ = _interopRequireDefault(require("./components/FAQ"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
 * Put all your regular jQuery in here.
 */
jQuery(document).ready(function ($) {
  (0, _Menu.default)($);
  (0, _SmoothScroll.default)($);
  (0, _Videos.showVideo)($);
  (0, _Popup.newsletterSignup)($);
  (0, _Videos.addPlaceholderInternalPages)($);
  (0, _Popup.popup)($);
  (0, _MobileMenu.default)($);
  (0, _ScrollTop.default)($);
  (0, _Redirects.default)();

  if ($("body").hasClass("page-template-page-contact")) {
    (0, _ContactPage.default)($);
  }

  if ($("body").hasClass("page-template-page-landing-marketing")) {
    (0, _LandingPage.default)($);
  }

  if ($("body").hasClass("single-post")) {
    (0, _Blog.addLightbox)($);
    (0, _Blog.addImageZoom)($);
  }

  if ($("body").hasClass("home")) {
    (0, _Slider.default)($);
  }

  if ($("body").hasClass("firms-template-page-secured")) {
    (0, _Trust.trustChosen)($);
    (0, _Trust.removeTitleQs)($);
    (0, _Trust.createTrust)($);
    (0, _Trust.removeCreate)($);
    (0, _Trust.addCheck)($);
  }

  if ($("iframe").length > 0 && $("iframe").attr("src").includes("acuity")) {
    (0, _Scheduler.default)($);
  }

  if ($("body").hasClass("page-template-page-faq")) {
    (0, _FAQ.default)($);
  }
});
/* end of as page load scripts */

window.onload = _Videos.loadIframe;
},{"./components/Menu":"components/Menu.js","./components/SmoothScroll":"components/SmoothScroll.js","./components/Videos":"components/Videos.js","./components/Popup":"components/Popup.js","./components/MobileMenu":"components/MobileMenu.js","./components/ScrollTop":"components/ScrollTop.js","./components/Redirects":"components/Redirects.js","./components/ContactPage":"components/ContactPage.js","./components/Blog":"components/Blog.js","./components/Slider":"components/Slider.js","./components/Trust":"components/Trust.js","./components/Scheduler":"components/Scheduler.js","./components/LandingPage":"components/LandingPage.js","./components/FAQ":"components/FAQ.js"}],"../../node_modules/parcel-bundler/src/builtins/hmr-runtime.js":[function(require,module,exports) {
var global = arguments[3];
var OVERLAY_ID = '__parcel__error__overlay__';
var OldModule = module.bundle.Module;

function Module(moduleName) {
  OldModule.call(this, moduleName);
  this.hot = {
    data: module.bundle.hotData,
    _acceptCallbacks: [],
    _disposeCallbacks: [],
    accept: function (fn) {
      this._acceptCallbacks.push(fn || function () {});
    },
    dispose: function (fn) {
      this._disposeCallbacks.push(fn);
    }
  };
  module.bundle.hotData = null;
}

module.bundle.Module = Module;
var checkedAssets, assetsToAccept;
var parent = module.bundle.parent;

if ((!parent || !parent.isParcelRequire) && typeof WebSocket !== 'undefined') {
  var hostname = "" || location.hostname;
  var protocol = location.protocol === 'https:' ? 'wss' : 'ws';
  var ws = new WebSocket(protocol + '://' + hostname + ':' + "59289" + '/');

  ws.onmessage = function (event) {
    checkedAssets = {};
    assetsToAccept = [];
    var data = JSON.parse(event.data);

    if (data.type === 'update') {
      var handled = false;
      data.assets.forEach(function (asset) {
        if (!asset.isNew) {
          var didAccept = hmrAcceptCheck(global.parcelRequire, asset.id);

          if (didAccept) {
            handled = true;
          }
        }
      }); // Enable HMR for CSS by default.

      handled = handled || data.assets.every(function (asset) {
        return asset.type === 'css' && asset.generated.js;
      });

      if (handled) {
        console.clear();
        data.assets.forEach(function (asset) {
          hmrApply(global.parcelRequire, asset);
        });
        assetsToAccept.forEach(function (v) {
          hmrAcceptRun(v[0], v[1]);
        });
      } else if (location.reload) {
        // `location` global exists in a web worker context but lacks `.reload()` function.
        location.reload();
      }
    }

    if (data.type === 'reload') {
      ws.close();

      ws.onclose = function () {
        location.reload();
      };
    }

    if (data.type === 'error-resolved') {
      console.log('[parcel] ✨ Error resolved');
      removeErrorOverlay();
    }

    if (data.type === 'error') {
      console.error('[parcel] 🚨  ' + data.error.message + '\n' + data.error.stack);
      removeErrorOverlay();
      var overlay = createErrorOverlay(data);
      document.body.appendChild(overlay);
    }
  };
}

function removeErrorOverlay() {
  var overlay = document.getElementById(OVERLAY_ID);

  if (overlay) {
    overlay.remove();
  }
}

function createErrorOverlay(data) {
  var overlay = document.createElement('div');
  overlay.id = OVERLAY_ID; // html encode message and stack trace

  var message = document.createElement('div');
  var stackTrace = document.createElement('pre');
  message.innerText = data.error.message;
  stackTrace.innerText = data.error.stack;
  overlay.innerHTML = '<div style="background: black; font-size: 16px; color: white; position: fixed; height: 100%; width: 100%; top: 0px; left: 0px; padding: 30px; opacity: 0.85; font-family: Menlo, Consolas, monospace; z-index: 9999;">' + '<span style="background: red; padding: 2px 4px; border-radius: 2px;">ERROR</span>' + '<span style="top: 2px; margin-left: 5px; position: relative;">🚨</span>' + '<div style="font-size: 18px; font-weight: bold; margin-top: 20px;">' + message.innerHTML + '</div>' + '<pre>' + stackTrace.innerHTML + '</pre>' + '</div>';
  return overlay;
}

function getParents(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return [];
  }

  var parents = [];
  var k, d, dep;

  for (k in modules) {
    for (d in modules[k][1]) {
      dep = modules[k][1][d];

      if (dep === id || Array.isArray(dep) && dep[dep.length - 1] === id) {
        parents.push(k);
      }
    }
  }

  if (bundle.parent) {
    parents = parents.concat(getParents(bundle.parent, id));
  }

  return parents;
}

function hmrApply(bundle, asset) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (modules[asset.id] || !bundle.parent) {
    var fn = new Function('require', 'module', 'exports', asset.generated.js);
    asset.isNew = !modules[asset.id];
    modules[asset.id] = [fn, asset.deps];
  } else if (bundle.parent) {
    hmrApply(bundle.parent, asset);
  }
}

function hmrAcceptCheck(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (!modules[id] && bundle.parent) {
    return hmrAcceptCheck(bundle.parent, id);
  }

  if (checkedAssets[id]) {
    return;
  }

  checkedAssets[id] = true;
  var cached = bundle.cache[id];
  assetsToAccept.push([bundle, id]);

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    return true;
  }

  return getParents(global.parcelRequire, id).some(function (id) {
    return hmrAcceptCheck(global.parcelRequire, id);
  });
}

function hmrAcceptRun(bundle, id) {
  var cached = bundle.cache[id];
  bundle.hotData = {};

  if (cached) {
    cached.hot.data = bundle.hotData;
  }

  if (cached && cached.hot && cached.hot._disposeCallbacks.length) {
    cached.hot._disposeCallbacks.forEach(function (cb) {
      cb(bundle.hotData);
    });
  }

  delete bundle.cache[id];
  bundle(id);
  cached = bundle.cache[id];

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    cached.hot._acceptCallbacks.forEach(function (cb) {
      cb();
    });

    return true;
  }
}
},{}]},{},["../../node_modules/parcel-bundler/src/builtins/hmr-runtime.js","scripts.js"], null)
//# sourceMappingURL=/scripts.min.js.map